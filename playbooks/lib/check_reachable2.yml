- hosts: "{{ servers }}"
  become: no
  gather_facts: no

  tasks:
    - name: wait for ssh to be available
      local_action: wait_for host="{{ ansible_ssh_host }}"
                             port=22 delay=10 timeout=300

